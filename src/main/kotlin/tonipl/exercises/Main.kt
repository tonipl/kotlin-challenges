import tonipl.exercises.utils.solution

fun main() {
    val height = 3.0
    val bounce = 0.66
    val window = 1.5

    println("Hello World!")
    println("Result of the exercise with height = $height, bounce = $bounce and window = $window: "
            + solution(height, bounce, window))
}
