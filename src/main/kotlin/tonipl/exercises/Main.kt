import tonipl.exercises.utils.RemovedNumbers

fun main() {
    val parameter: Long = 26
    println("Hello World!")
    println("Result of the exercise with parameter $parameter: "
            + RemovedNumbers.solution(parameter).contentDeepToString())
}
